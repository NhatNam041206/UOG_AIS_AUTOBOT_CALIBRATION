# Core computer-vision dependency (pre-built wheel on Jetson Nano)
opencv-python-headless>=4.5.0

# NumPy is bundled with OpenCV but listed explicitly for clarity
numpy>=1.21.0

# Environment-based configuration loading
python-dotenv>=1.0.0
paho-mqtt>=2.1.0

matplotlib>=3.4.0

# HTTPS MJPEG stream server
fastapi>=0.110.0
uvicorn>=0.29.0
cryptography>=42.0.0

# Gamepad / keyboard input (MiniPC)
evdev>=1.6.1

# ESP32 USB-serial actuator bridge (MiniPC)
pyserial>=3.5
